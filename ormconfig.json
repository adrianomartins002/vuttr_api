{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "vuttr",
  "entities": [
    "./src/models/*.{js,ts}"
  ],
  "migrations": [
    "./src/database/migrations/*.{js,ts}"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations/"
  }
}
